# Exact versions this project was built and tested against (2026-09-04).
# For a reproducible environment:
#     uv venv && uv pip install -r requirements.txt
# For a looser install that picks up newer compatible releases, use
# pyproject.toml instead: uv pip install -e ".[dev]"

# --- direct dependencies ---
pandas==3.0.5
numpy==2.5.2
requests==2.34.2
PyYAML==6.0.3
jugaad-data==0.35.5

# --- test dependencies ---
pytest==9.1.1

# --- transitive, pinned for reproducibility ---
appdirs==1.4.4
beautifulsoup4==4.15.0
brotli==1.2.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
idna==3.19
iniconfig==2.3.0
lxml==6.1.3
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
python-dateutil==2.9.0.post0
six==1.17.0
soupsieve==2.9.2
typing_extensions==4.16.0
urllib3==2.7.0
streamlit>=1.40
